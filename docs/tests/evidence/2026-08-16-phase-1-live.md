# Phase 1 Live Evidence — 2026-08-16

Status: passed locally.

This is a sanitized summary of the Final Cut Pro E2E run. It intentionally
omits user-specific filesystem paths, private media, and raw diagnostic logs.

## Result

Final Cut Pro reopened with the `Playhead Phase 1 E2E` project. The rebuilt
Workflow Extension launched, created its local socket, and served live state
to the MCP runtime. No export or timeline mutation was performed.

## Observed state

- backend: `workflow-extension-ipc`
- editor: Final Cut Pro
- project: `Playhead Phase 1 E2E`
- sequence: `Playhead Phase 1 E2E`
- playhead: `780780 / 720000`
- sequence duration: `600600 / 120000`
- sequence start: `0 / 1`
- frame duration: `1001 / 24000`
- revision: sequence `3`, id `rev-3`

## Observed live events

1. `active-sequence-changed`
2. `sequence-time-range-changed`
3. `playhead-changed`

The first bootstrap events may contain unavailable rational times with a zero
timescale while Final Cut finishes publishing state. The current live
snapshot returned valid values.

## Additional checks

- `npm test`: 12 passed, 0 failed.
- `npm run xcode:check`: Xcode 16.4 / build 16F6, SDK 15.5.
- `nix flake check path:./nix --no-update-lock-file`: passed.
- `codesign --verify --deep --strict`: passed.
